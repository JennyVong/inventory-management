<h1> <%= @warehouse.name %> </h1>

<span> <%= @warehouse.location %> </span>

<ul>
  <li><%= link_to "Edit", edit_warehouse_path(@warehouse) %></li>
  <li><%= link_to "Destroy", warehouse_path(@warehouse), data: {
                    turbo_method: :delete,
                    turbo_confirm: "Are you sure?"
                  } %></li>
</ul>

<%= link_to "home", root_path %>