<h1><%= @item.name %></h1>

<span><%= @item.desc %></span><br>

<span> $<%= @item.price %> | <%= @item.quantity %> in stock</span>

<ul>
  <li><%= link_to "Edit", edit_item_path(@item) %></li>
  <li><%= link_to "Destroy", item_path(@item), data: {
                    turbo_method: :delete,
                    turbo_confirm: "Are you sure?"
                  } %></li>
</ul>

<%= link_to "home", root_path %>